# Rust build output
/target
**/*.rs.bk

# Note: Cargo.lock IS committed (reproducible builds — see docs/DECISION_LOG.md D-0006).

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code local session/agent state (worktrees, etc.) — not project source
.claude/

# Python bytecode cache (tools/*.py: mininet_nav.py, check_governance.py)
__pycache__/
*.pyc
