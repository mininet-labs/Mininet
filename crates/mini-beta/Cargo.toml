[package]
name = "mini-beta"
version = "0.0.1"
description = "Open-Beta coordination objects and resettable Beta MINI test currency. Deliberately isolated from production value, settlement, treasury, chain, and governance authority."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
did-mini = { path = "../did-mini" }
mini-objects = { path = "../mini-objects" }
mini-store = { path = "../mini-store" }
