[package]
name = "mini-provenance"
version = "0.0.1"
description = "SLSA/in-toto-style build provenance as real, signed, content-addressed objects (self-hosted forge spine Batch 2a, D-0068) -- generalizes mini-forge's independent-attestation pattern from release artifacts to the build stage itself."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
did-mini = { path = "../did-mini" }
mini-forge = { path = "../mini-forge" }
mini-objects = { path = "../mini-objects" }
mini-store = { path = "../mini-store" }
