[package]
name = "mini-storage-fraud"
version = "0.0.1"
description = "Experimental cross-identity storage-fraud detection (roadmap #42): identity-bound replica ids, independently-audited registration claims over a full mini-porep SealCommitment, a uniqueness registry, and unattributed replica-conflict evidence. Detects and proves only -- no penalty, no consequence, no attribution. Unaudited prototype gated behind D-0047; see docs/design/storage-fraud-detection.md."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
did-mini = { path = "../did-mini" }
mini-crypto = { path = "../mini-crypto" }
mini-porep = { path = "../mini-porep" }
mini-spacetime = { path = "../mini-spacetime" }
