[package]
name = "mini-storage"
version = "0.0.1"
description = "Mutually-signed storage-served receipts for Mininet: proof that one verified identity root's device served content to another, offline-verifiable and reward-eligible."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
mini-crypto = { path = "../mini-crypto" }
did-mini = { path = "../did-mini" }
mini-objects = { path = "../mini-objects" }
