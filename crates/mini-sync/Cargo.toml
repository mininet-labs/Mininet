[package]
name = "mini-sync"
version = "0.0.1"
description = "Store-and-forward replication for Mininet: bucketed set reconciliation, verified ingest, and KEL distribution over any bearer — offline-first, resumable by idempotence."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
mini-crypto = { path = "../mini-crypto" }
did-mini = { path = "../did-mini" }
mini-objects = { path = "../mini-objects" }
mini-store = { path = "../mini-store" }
mini-bearer = { path = "../mini-bearer" }
