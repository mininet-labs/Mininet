[package]
name = "mini-update"
version = "0.0.1"
description = "Local, explicit update-adoption state machine: wraps mini-forge's release verification gates. No forced update, no kill path — adoption is always the device owner's local act."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
did-mini = { path = "../did-mini" }
mini-objects = { path = "../mini-objects" }
mini-store = { path = "../mini-store" }
mini-forge = { path = "../mini-forge" }

[dev-dependencies]
mini-crypto = { path = "../mini-crypto" }
mini-media = { path = "../mini-media" }
